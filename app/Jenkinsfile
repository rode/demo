pipeline {
    agent {
        kubernetes {
            yamlFile "app/jenkins-agent.yaml"
        }
    } 
    stages {
        stage('echo') {
            steps {
              container('kaniko') {
                sh 'executor -c app --skip-tls-verify -d harbor.localhost/library/rode-demo-node-app:latest'
              }
            }
        }
    }
}
