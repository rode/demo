pipeline {
    agent {
        kubernetes {
            yamlFile "app/jenkins-agent.yaml"
        }
    } 
    stages {
        stage('echo') {
            steps {
              container('docker') {
                echo 'hello world'
                echo 'ls'
                sh 'docker build -t node/sample-web-app ./app'
                sh 'docker images'
              }
            }
        }
    }
}
