pipeline {
    agent {
        kubernetes {
            yamlFile "app/jenkins-agent.yaml"
        }
    } 
    stages {
        stage('echo') {
            steps {
              container('kaniko') {
                echo 'hello world'
                sh 'executor -c app --insecure -d harbor-harbor-registry.harbor.svc.cluster.local:5000/docker/node-app:latest'
              }
            }
        }
    }
}
